# MySQL 8 connection string. Use a dedicated, least-privilege account in production.
DATABASE_URL=mysql://schedule_user:change-me@127.0.0.1:3306/wzbc_schedule

# Generate at least 32 random bytes for production; never commit the real value.
BETTER_AUTH_SECRET=replace-with-a-strong-random-secret
BETTER_AUTH_URL=http://localhost:3000
